apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "sms-checker.fullname" . }}-frontend
  labels:
    release: {{ .Release.Name }}
spec:
  namespaceSelector:
    matchNames:
      - {{ .Release.Namespace }}
  selector:
    matchLabels:
      app: sms-checker
      component: frontend
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
