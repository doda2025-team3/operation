apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "team3_chart.fullname" . }}-test-connection"
  labels:
    {{- include "team3_chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "team3_chart.fullname" . }}:{{ .Values.modelService.service.port }}']
  restartPolicy: Never
