apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: app-alerts
  labels:
    release: {{ .Release.Name }}
spec:
  groups:
    - name: operation.rules
      rules:
        - alert: HighRequestRate
          expr: sum(rate(sms_predictions_total[1m])) * 60 > 7
          for: 1m
          labels:
            severity: warning
            namespace: {{ .Release.Namespace }}
          annotations:
            summary: "High request rate on {{ `{{ $labels.instance }}` }}"
            description: >
              Service received more than 7 HTTP requests/minute for >= 1 minutes.
